plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}
android {
    namespace  'com.alisons.aquaflow'
    compileSdk 35
    ndkVersion = "26.1.10909125"

    def keystorePropertiesFile = rootProject.file("android/key.properties")
    def keystoreProperties = new Properties()
   if (keystorePropertiesFile.exists()) {
    keystoreProperties.load(new FileInputStream(keystorePropertiesFile))
} else {
    println "Keystore properties file not found!"
}

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
        coreLibraryDesugaringEnabled true
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        applicationId = "com.alisons.aquaflow"
        minSdkVersion 21
        targetSdkVersion 35
        multiDexEnabled true
        versionCode = 1
        versionName "1.0.0"
    }

  signingConfigs {
        release {
            keyAlias 'upload' 
            keyPassword '123456' 
            storeFile file('upload-keystore.jks')  
            storePassword '123456' 
        }
    }

    buildTypes {
    release {
        signingConfig signingConfigs.release
        shrinkResources true    // Shrink resources to reduce APK size
        minifyEnabled true      // Enable minification (obfuscates and removes unused code)
        proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro' // Make sure you have a proguard file
        ndk {
                debugSymbolLevel 'FULL'  // Generate native debug symbols
            }   
    }
}
}



dependencies {
    coreLibraryDesugaring("com.android.tools:desugar_jdk_libs:2.0.3")
}

flutter {
    source = "../.."
}
